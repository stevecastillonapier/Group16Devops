version: '3'
services:
  # Application Dockerfile is in same folder which is .
  app:
    build: .
    depends_on:
      - db
    environment:
      - DB_HOST=db

  # db is is db folder
  db:
    build: db/.
    ports:
      - 3306:3306
    restart: always